<nav class="nav">
  <h1><a href="/Home">🐶Como Perros y Gatos🐱</a></h1>

  <ul class="nav-list">
    <li><a href="/Like">Me Gusta</a></li>
    <li><a href="/Favorites">Favoritos</a></li>

    <li class="dropdown">
      <a href="javascript:void(0)" class="dropbtn" id="nav-icon1">
        <span></span>
        <span></span>
        <span></span>
      </a>

      <ul class="dropdown-content">
        <li><a href="/item1">Perfil</a></li>
        <li><a href="/item2">Configuracion</a></li>
        <li><a href="/item3">Cerrar Sesión</a></li>
      </ul>
    </li>
  </ul>
</nav>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const dropdown = document.querySelector(".dropdown")!;
    const boton = dropdown.querySelector("#nav-icon1")!;

    function toggleMenu() {
      dropdown.classList.toggle("open");
      boton.classList.toggle("open");
    }

    boton.addEventListener("click", toggleMenu);
  });
</script>

<style>
  /* ===========================
     NAV PRINCIPAL (ESCRITORIO)
     =========================== */
  .nav {
    position: fixed;
    top: 2rem;
    left: 50%;
    transform: translateX(-50%);
    max-width: 90%;
    width: 100%;
    height: 4rem;
    margin: 0 auto;
    background-color: #f69a0b;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-radius: 1rem;
    z-index: 1000;
  }

  .nav h1 {
    margin: 0;
    font-size: 1.25rem;
  }
  .nav h1 a {
    font-size: 2rem;
    text-decoration: none;
    color: inherit;
  }

  .nav-list {
    list-style: none;
    display: flex;
    gap: 1.5rem;
    margin: 0;
    padding: 0;
    align-items: center;
  }

  .nav-list a {
    font-size: 1.5rem;
    text-decoration: none;
    color: black;
    padding: 0.5rem 0.75rem;
    border-radius: 0.5rem;
    transition: background 0.2s;
  }

  .nav-list a:hover {
    background-color: rgba(0, 0, 0, 0.1);
  }

  /* ===========================
     DROPDOWN CON ÍCONO
     =========================== */
  .dropdown {
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .dropbtn {
    cursor: pointer;
    display: inline-block;
    padding: 0.5rem 0.75rem;
    border-radius: 0.5rem;
    color: black;
    text-decoration: none;
    transition: background 0.2s;
    font-size: 0.95rem;
  }

  .dropbtn:hover {
    background-color: rgba(0, 0, 0, 0.1);
  }

  .dropdown-content {
    display: none;
    position: absolute;
    top: calc(100% + 0.25rem);
    right: 0;
    background-color: #fdbf5c;
    min-width: 12rem;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
    border-radius: 0.5rem;
    overflow: hidden;
    z-index: 1001;
  }

  .dropdown-content li {
    list-style: none;
  }

  .dropdown-content a {
    display: block;
    padding: 1rem;
    color: #333;
    text-decoration: none;
    transition: background 0.2s;
  }

  .dropdown-content a:hover {
    background-color: #f69a0b;
  }

  .dropdown.open .dropdown-content {
    display: block;
  }

  /* ===========================
     ESTILOS “HAMBURGER”
     =========================== */
  #nav-icon1 {
    width: 24px;
    height: 18px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    cursor: pointer;
    margin: 0;
    padding: 0;
    position: relative;
    z-index: 1002;
    transition: all 0.3s ease-in-out;
  }

  #nav-icon1 span {
    display: block;
    width: 100%;
    height: 2px;
    background: #d3531a;
    border-radius: 1px;
    transition:
      transform 0.3s ease-in-out,
      opacity 0.3s ease-in-out;
    transform-origin: center center;
  }

  #nav-icon1 span:nth-child(1) {
    transform: translateY(0);
  }
  #nav-icon1 span:nth-child(2) {
    transform: translateY(8px);
  }
  #nav-icon1 span:nth-child(3) {
    transform: translateY(16px);
  }

  #nav-icon1.open span:nth-child(1) {
    transform: translateY(8px) rotate(45deg);
  }
  #nav-icon1.open span:nth-child(2) {
    opacity: 0;
    transform: translateY(8px);
  }
  #nav-icon1.open span:nth-child(3) {
    transform: translateY(8px) rotate(-45deg);
  }

  /* ===========================
     MEDIA QUERY: TABLET (≤ 768px)
     =========================== */
  @media (max-width: 768px) {
    .nav {
      padding: 0 1.5rem;
      height: 3.5rem;
    }

    .nav h1 a {
      font-size: 1.75rem;
    }

    .nav-list a {
      font-size: 1.25rem;
      padding: 0.4rem 0.6rem;
    }

    /* El menú dropdown se muestra igual, pero ajustamos tamaños */
    #nav-icon1 {
      width: 20px;
      height: 15px;
    }
    #nav-icon1 span:nth-child(2) {
      transform: translateY(6px);
    }
    #nav-icon1 span:nth-child(3) {
      transform: translateY(12px);
    }

    #nav-icon1.open span:nth-child(1) {
      transform: translateY(6px) rotate(45deg);
    }
    #nav-icon1.open span:nth-child(2) {
      transform: translateY(6px);
    }
    #nav-icon1.open span:nth-child(3) {
      transform: translateY(6px) rotate(-45deg);
    }
  }

  /* ===========================
     MEDIA QUERY: MÓVIL (≤ 480px)
     =========================== */
  @media (max-width: 480px) {
    .nav {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      padding: 0 1rem;
      height: 3rem;
    }

    .nav h1 a {
      font-size: 1.5rem;
    }

    /* Ocultamos la lista completa */
    .nav-list {
      display: none;
    }

    /* Mostramos solo el título y el ícono */
    .dropdown {
      margin-left: auto;
    }

    /* Estilos del dropdown sobre fondo completo */
    .dropdown-content {
      position: fixed;
      top: 3rem; /* justo debajo del nav */
      right: 0;
      width: 100%;
      max-width: 250px;
      background-color: #fdbf5c;
      border-radius: 0 0 10px 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
    }

    .dropdown-content a {
      padding: 0.75rem 1rem;
      font-size: 1rem;
    }

    /* Ajustamos el ícono aún más pequeño */
    #nav-icon1 {
      width: 18px;
      height: 14px;
    }
    #nav-icon1 span:nth-child(2) {
      transform: translateY(5px);
    }
    #nav-icon1 span:nth-child(3) {
      transform: translateY(10px);
    }

    #nav-icon1.open span:nth-child(1) {
      transform: translateY(5px) rotate(45deg);
    }
    #nav-icon1.open span:nth-child(2) {
      transform: translateY(5px);
    }
    #nav-icon1.open span:nth-child(3) {
      transform: translateY(5px) rotate(-45deg);
    }
  }
</style>
