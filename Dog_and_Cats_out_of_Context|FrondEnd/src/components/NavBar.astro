<nav class="nav">
  <h1><a href="/Home">🐶Como Perros y Gatos🐱</a></h1>

  <ul class="nav-list">
    <li><a href="/Like">Me Gusta</a></li>
    <li><a href="/Favorites">Favoritos</a></li>

    <li class="dropdown">
      <!-- Ícono “hamburger” -->

      <!-- “Menú ▾” sólo como indicador; no hace falta usar hover -->
      <a href="javascript:void(0)" class="dropbtn" id="nav-icon1">
        <span></span>
        <span></span>
        <span></span>
      </a>

      <!-- Contenido oculto por defecto -->
      <ul class="dropdown-content">
        <li><a href="/item1">Perfil</a></li>
        <li><a href="/item2">Configuracion</a></li>
        <li><a href="/item3">Cerra Secion</a></li>
      </ul>
    </li>
  </ul>
</nav>

<script>
  // Al cargar DOM, asignamos el clic al ícono y al texto “Menú ▾”
  document.addEventListener("DOMContentLoaded", () => {
    const dropdown = document.querySelector(".dropdown")!; // afirmo que no es null
    const boton = dropdown.querySelector("#nav-icon1")!;
    const label = dropdown.querySelector(".dropbtn")!;

    function toggleMenu() {
      dropdown.classList.toggle("open");
      boton.classList.toggle("open");
    }

    boton.addEventListener("click", toggleMenu);
    label.addEventListener("click", toggleMenu);
  });
</script>

<style>
  /* ===========================
     NAV PRINCIPAL
     =========================== */
  .nav {
    position: fixed;
    top: 2rem;
    left: 50%;
    transform: translateX(-50%);
    max-width: 90%;
    width: 100%;
    height: 4rem;
    margin: 0 auto;
    background-color: #f69a0b;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-radius: 1rem;
    z-index: 1000;
  }

  .nav h1 {
    margin: 0;
    font-size: 1.25rem;
  }
  .nav h1 a {
    font-size: 2rem;
    text-decoration: none; /* Quita la línea */
    color: inherit; /* Mantiene el color del h1 */
  }

  .nav-list {
    list-style: none;
    display: flex;
    gap: 1.5rem;
    margin: 0;
    padding: 0;
    align-items: center;
  }

  .nav-list a {
    font-size: 1.5rem;
    text-decoration: none;
    color: black;
    padding: 0.5rem 0.75rem;
    border-radius: 0.5rem;
    transition: background 0.2s;
  }

  .nav-list a:hover {
    background-color: rgba(0, 0, 0, 0.1);
  }

  /* ===========================
     DROPDOWN CON ÍCONO
     =========================== */
  .dropdown {
    position: relative; /* para alinear el contenido absoluto */
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  /* Estilos del enlace “Menú ▾” */
  .dropbtn {
    cursor: pointer;
    display: inline-block;
    padding: 0.5rem 0.75rem;
    border-radius: 0.5rem;
    color: black;
    text-decoration: none;
    transition: background 0.2s;
    font-size: 0.95rem;
  }

  .dropbtn:hover {
    background-color: rgba(0, 0, 0, 0.1);
  }

  /* Contenedor de las opciones: oculto por defecto */
  .dropdown-content {
    display: none;
    position: absolute;
    top: calc(100% + 0.25rem); /* justo debajo de “Menú” */
    right: 0; /* alineado a la derecha de .dropdown */
    background-color: #fdbf5c;
    min-width: 12rem;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
    border-radius: 0.5rem;
    overflow: hidden;
    z-index: 1001;
  }

  /* Cada opción interna */
  .dropdown-content li {
    list-style: none;
  }

  .dropdown-content a {
    display: block;
    padding: 1rem;
    color: #333;
    text-decoration: none;
    transition: background 0.2s;
  }

  .dropdown-content a:hover {
    background-color: #f69a0b;
  }

  /* Cuando .dropdown tenga la clase open, mostramos el contenido */
  .dropdown.open .dropdown-content {
    display: block;
  }

  /* ===========================
     ESTILOS “HAMBURGER” (#nav-icon1)
     =========================== */
  #nav-icon1 {
    width: 24px; /* ancho pequeño */
    height: 1.8rem; /* alto pequeño */
    position: relative;
    margin: 0;
    -webkit-transform: rotate(0deg);
    -moz-transform: rotate(0deg);
    -o-transform: rotate(0deg);
    transform: rotate(0deg);
    -webkit-transition: 0.5s ease-in-out;
    -moz-transition: 0.5s ease-in-out;
    -o-transition: 0.5s ease-in-out;
    transition: 0.5s ease-in-out;
    cursor: pointer;
  }

  #nav-icon1 span {
    margin: 0;
    padding: 0;
    display: block;
    position: absolute;
    height: 9px;
    width: 100%;
    background: #d3531a;
    border-radius: 9px;
    opacity: 1;
    left: 0;
    -webkit-transform: rotate(0deg);
    -moz-transform: rotate(0deg);
    -o-transform: rotate(0deg);
    transform: rotate(0deg);
    -webkit-transition: 0.25s ease-in-out;
    -moz-transition: 0.25s ease-in-out;
    -o-transition: 0.25s ease-in-out;
    transition: 0.25s ease-in-out;
  }

  #nav-icon1 span:nth-child(1) {
    top: 0px;
  }

  #nav-icon1 span:nth-child(2) {
    top: 18px;
  }

  #nav-icon1 span:nth-child(3) {
    top: 36px;
  }

  #nav-icon1.open span:nth-child(1) {
    top: 18px;
    -webkit-transform: rotate(135deg);
    -moz-transform: rotate(135deg);
    -o-transform: rotate(135deg);
    transform: rotate(135deg);
  }

  #nav-icon1.open span:nth-child(2) {
    opacity: 0;
    left: -60px;
  }

  #nav-icon1.open span:nth-child(3) {
    top: 18px;
    -webkit-transform: rotate(-135deg);
    -moz-transform: rotate(-135deg);
    -o-transform: rotate(-135deg);
    transform: rotate(-135deg);
  }
</style>
